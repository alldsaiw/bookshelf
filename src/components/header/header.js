import './header.css'
import HeaderMenu from '../header-menu/header-menu';

const Header = () => {
    return (
        <header className="header__wrapper">
            <HeaderMenu/>
        </header>
    )
}

export default Header;