import './slider.css'
import SliderItem from '../slider-item/slider-item';

const Slider = () => {
    return (
        <div className="slider">
            <SliderItem/>
            <SliderItem/>
            <SliderItem/>
        </div>
    )
};

export default Slider;
